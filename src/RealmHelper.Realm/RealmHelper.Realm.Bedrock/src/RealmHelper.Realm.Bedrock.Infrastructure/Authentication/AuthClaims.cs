namespace RealmHelper.Realm.Bedrock.Infrastructure.Authentication;

public class AuthClaims
{
    public const string XstsToken = nameof(XstsToken);
    
    public const string UserHash = nameof(UserHash);
}